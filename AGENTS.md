# AGENTS.md

## 專案性質

這是一份台灣碩士論文的 repo，主檔案是根目錄的 `論文草稿＿20250323.docx`（Word 2007+ 格式）。題目：《運用計畫行為理論探討台灣遊客在過度旅遊情境下之綠色旅遊行為意圖》。這不是軟體專案——沒有程式碼、沒有測試套件，你的工作是學術寫作與問卷設計。

## 現況（開始工作前務必先讀）

問卷架構已完成一輪重新設計（信念構面納入模型、過度旅遊認知定位為調節變數、綠色旅遊行為定案五大領域、行為題項改為分項意圖），並派了四路背景研究查證文獻。**設計已定案，但論文正文和問卷本體都還沒有實際改寫**——這是你要做的事。

**開工前依序讀完這四份文件，不要跳過**：

1. `docs/handoff-report.md` — 整個重構專案的敘事總覽：問題盤點、決策摘要、四份研究報告的關鍵結論、完整的下一步任務清單（第 4 節）
2. `CONTEXT.md` — 論文所有構念的權威定義。與論文正文寫法衝突時，以此為準
3. `docs/adr/0001-ot-as-moderator-on-tail-paths.md`、`docs/adr/0002-five-domains-and-domain-intentions.md` — 兩個核心設計決策的完整論證
4. `docs/research/a~d-*.md` — 四份文獻查證報告，改寫問卷題項時的題項草稿與來源都在這裡

`docs/handoff-report.md` 第 4 節列了完整任務清單（摘要與緒論改寫、假設 H1~H6 改寫、問卷 13 張表重寫、方法論補多群組 SEM 說明），照那份清單做即可。

## 硬性規則

1. **絕不編造文獻或題項出處。** 前面四路研究報告裡標「未能查證」的文獻，你要寫進論文正文前必須自己找到原文核實；標記不可以被直接當成已查證事實抄進去。
2. **不要臆測填空。** 遇到文獻查不到、數據對不上、或某個決策沒有被記錄下來，先在對話中問清楚，不要自己猜一個答案寫進論文。
3. **CONTEXT.md 是詞彙權威。** 論文用詞（構念名稱、五大領域名稱、變數符號）一律以 CONTEXT.md 為準；若你認為需要修改定義，先改 CONTEXT.md，不要讓論文正文和 CONTEXT.md 各說各話。
4. **重大且難逆轉的設計決策要開新 ADR**（仿照 `docs/adr/0001`、`0002` 的格式：背景、考慮過的方案、決策、理由、代價與風險），不要只改完論文就算了、沒留下決策紀錄。
5. 全文使用繁體中文（台灣用語），APA 第七版引用格式（論文既有的參考文獻章節已是這個格式）。

## 如何編輯 .docx

`.docx` 是一個 ZIP 壓縮的 XML 檔案集合，本機沒有 `pandoc`，但有 LibreOffice（`soffice`/`libreoffice`）與 Python 3。建議流程：

```bash
# 讀取內容：解壓縮後直接讀 XML，或用 LibreOffice 轉純文字/PDF 檢視排版
unzip -q "論文草稿＿20250323.docx" -d unpacked/
# word/document.xml 是正文；<w:t> 標籤內是可見文字，其餘是格式與 TOC 欄位代碼雜訊

# 編輯：直接改 unpacked/word/document.xml，改完重新打包
cd unpacked && zip -Xr ../論文草稿＿20250323.docx . && cd ..

# 驗證排版有沒有跑掉：轉 PDF 再轉圖片，用視覺方式確認
soffice --headless --convert-to pdf "論文草稿＿20250323.docx"
pdftoppm -jpeg -r 100 "論文草稿＿20250323.pdf" page
# 再讀取 page-*.jpg 檢查
```

Word 常把同一句話拆成多個 `<w:r>` run（版本追蹤、拼字檢查標記），肉眼看到的一句話在 XML 裡可能找不到連續字串，改之前先確認 run 有沒有被拆散。

如果有 docx 專用工具（例如 Claude Code 的 docx skill 提供的 `merge_runs.py`、`validate.py`）可用就優先用；沒有的話上述手動流程也足夠應付這個專案規模。

## Git

工作分支與 PR 慣例請看 `.claude/settings.json`、既有的 commit 歷史抓風格（英文 commit message、說明「為什麼」而非「做了什麼」）。目前 `main` 就是最新狀態，沒有其他長期分支在跑。
